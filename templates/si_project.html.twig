{#
/**
 * @file si_project.html.twig
 * Default theme implementation to present Si_project data.
 *
 * This template is used when viewing Si_project pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_si_project()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('si_project') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
